# Mixed-disease fictional catalog eligibility

Allow optional per-variant `diseases`, a nonempty unique subset of catalog scope. Omission retains existing behavior. Patient eligibility must apply this restriction before matching/balancing; original disease values remain untouched. This prevents pathogen-specific antibiotic regimens from crossing into unrelated cohorts. Add focused regression coverage and synchronize the installed skill after verification.
